import { StyleSheet } from 'react-native';

import { THEME } from '../../common';

const { UNIT } = THEME;

export default StyleSheet.create({
  container: {
    marginTop: UNIT / 4,
    paddingLeft: UNIT,
    maxWidth: '100%',
  },
});
